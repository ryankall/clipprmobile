export enum AppointmentStatus {
    Confirmed = "confirmed",
    Expired = "expired",
    Cancelled = "cancelled",
    NoShow = "no_show",
    Pending = "pending",
}

export enum SubscriptionStatus {
    Basic = "basic",
    Premium = "premium",
}
